<!DOCTYPE html>
<html lang="en">

<head>
    <title>My amazing blog!</title>
    <meta charset="UTF-8" />
    <meta name="description" content="Wow, like and subscribe, it really helps the channel" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/water.css@2/out/water.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <style>
        .material-symbols-outlined {
          font-variation-settings:
          'FILL' 0,
          'wght' 400,
          'GRAD' 0,
          'opsz' 48
        }

        .playername p {
            margin : 0
        }

        .playername .spec {
            font-size : 0.5em
        }

        th {
            position: sticky;
            top: 0;
            background-color: #202b38;
        }
        </style>
</head>

<body>
    <nav role=" navigation">
        <div>
            <ul>
                <li><a href="p1.html">Phase 1</a></li>
                <li><a href="p2.html">Phase 2</a></li>
                <li><a href="p3.html">Phase 3</a></li>
            </ul>
        </div>
    </nav>
    <main>
       <table>
        <thead>
            <tr>
                <th width="100px">Player</th>
                {{range $.Slots}}
                <th width="32px"><img src="{{ getSlotImage . }}" /></th>
                {{end}}
            </tr>
        </thead>
        <tbody>        
        {{range $character := .Data }}
            <tr>
                <td class="playername">
                    <p style="color: {{getClassColor .Class}}">{{.Name}}</p>
                    <p class="spec">{{.Spec}}</p>
                </td>
                {{- range $slot := $.Slots}} 
                    {{$psd := getPhaseSlotData $character $.Phase $slot}} 
                    <td>{{- if $psd.InTier}}<span class="material-symbols-outlined">done</span>{{- end}}</td>
                {{- end}}
            </tr>
        {{end}}            
        </tbody>   
    </table>
    </main>
</body>

</html>