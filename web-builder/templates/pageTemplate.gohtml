<!DOCTYPE html>
<html lang="en">

<head>
    <title>Reset Loot!</title>
    <meta charset="UTF-8" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/water.css@2/out/water.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <script>const whTooltips = {colorLinks: true, iconizeLinks: true, renameLinks: true};</script>
    <script src="https://wow.zamimg.com/js/tooltips.js"></script>
   <style>
        .material-symbols-outlined {
          font-variation-settings:
          'FILL' 0,
          'wght' 400,
          'GRAD' 0,
          'opsz' 48
        }

        .playername p {
            margin : 0
        }

        .playername .spec {
            font-size : 0.5em
        }

        th {
            position: sticky;
            top: 0;
            background-color: #202b38;
        }

        .itemCell .items {
            visibility: hidden;
            width: 120px;
            background-color: black;
            color: #fff;
            text-align: center;
            padding: 5px 0;
            border-radius: 6px;
            position: absolute;
            z-index: 1;
        }

        .itemCell:hover > .items {
            visibility: visible;
        } 
        </style>
</head>

<body>
    <nav role=" navigation">
        <div>
            <ul>
                <li><a href="p1.html">Phase 1</a></li>
                <li><a href="p2.html">Phase 2</a></li>
                <li><a href="p3.html">Phase 3</a></li>
            </ul>
        </div>
    </nav>
    <main>
       <table>
        <thead>
            <tr>
                <th width="100px">Player</th>
                {{range $.Slots}}
                <th width="32px"><img src="{{ getSlotImage . }}" /></th>
                {{end}}
            </tr>
        </thead>
        <tbody>        
        {{range $character := .Data }}
            <tr>
                <td class="playername">
                    <p style="color: {{getClassColor .Class}}">{{.Name}}</p>
                    <p class="spec">{{.Spec}}</p>
                </td>
                {{- range $slot := $.Slots}} 
                    {{$psd := getPhaseSlotData $character $.Phase $slot}} 
                    <td class="itemCell">{{- if $psd.InTier}}<span class="material-symbols-outlined">done</span>{{- end}}
                        {{if $psd.Items}}
                            <div class="items">
                                {{- range $psd.Items}}
                                <a href="#" data-wowhead="item={{.ItemID}}&domain=wotlk">{{.ItemName}}</a><br />
                                {{end}}
                            </div>                           
                        {{end}}</td>

                  
                {{- end}}
            </tr>
        {{end}}            
        </tbody>   
    </table>
    </main>
</body>

</html>